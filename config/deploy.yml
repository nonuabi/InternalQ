# Name of your application. Used to uniquely configure containers.
service: internalq

# Name of the container image.
image: prod/internalq

# Deploy to these servers.
servers:
  web:
    hosts:
      - 13.206.218.103
    options:
      "add-host": host.docker.internal:host-gateway

  job:
    cmd: bundle exec sidekiq -q default
    hosts:
      - 13.206.218.103
    options:
      "add-host": host.docker.internal:host-gateway 


# Enable SSL auto certification via Let's Encrypt and allow for multiple apps on a single web server.
# Remove this section when using multiple web servers and ensure you terminate SSL at your load balancer.
#
# Domain: internalq.zezlab.com (A record in DNS points to EC2 IPv4 public IP)
# Note: If using Cloudflare, set encryption mode in SSL/TLS setting to "Full" to enable CF-to-app encryption.
proxy:
  ssl: true
  host: internalq.zezlab.com

# Credentials for your image host.
registry:
  # we are using AWS ECR
  server: 950771917805.dkr.ecr.ap-south-1.amazonaws.com
  username: AWS
  # Always use an access token rather than real password when possible.
  password:
    - KAMAL_REGISTRY_PASSWORD
# Inject ENV variables into containers (secrets come from .kamal/secrets).
env:
  secret:
    - RAILS_MASTER_KEY
    - INTERNALQ_DATABASE_PASSWORD
    - INTERNALQ_DATABASE_HOST
    - INTERNALQ_DATABASE_NAME
    - INTERNALQ_DATABASE_USERNAME
    - OPENAI_API_KEY
    - GEMINI_API_KEY
    - SLACK_CLIENT_ID
    - SLACK_CLIENT_SECRET
    - SLACK_SIGNING_SECRET
    - SLACK_REDIRECT_URI
    - GOOGLE_CLIENT_ID
    - GOOGLE_CLIENT_SECRET
    - DISABLE_EMAIL_AUTH
    - DEFAULT_MONTHLY_QUESTION_LIMIT
    - REDIS_URL
    - RUN_JOBS_INLINE
  clear:
    # Run the Solid Queue Supervisor inside the web server's Puma process to do jobs.
    # When you start using multiple servers, you should split out job processing to a dedicated machine.
    RAILS_ENV: production
    RAILS_LOG_TO_STDOUT: 'true'
    RAILS_SERVE_STATIC_FILES: 'true'


    # Set number of processes dedicated to Solid Queue (default: 1)
    # JOB_CONCURRENCY: 3

    # Set number of cores available to the application on each server (default: 1).
    # WEB_CONCURRENCY: 2

    # Match this to any external database server to configure Active Record correctly
    # Use internalq-db for a db accessory server on same machine via local kamal docker network.
    # DB_HOST: 192.168.0.2

    # Log everything from Rails
    # RAILS_LOG_LEVEL: debug

# Aliases are triggered with "bin/kamal <alias>". You can overwrite arguments on invocation:
# "bin/kamal logs -r job" will tail logs from the first server in the job section.
aliases:
  console: app exec --interactive --reuse "bin/rails console"
  shell: app exec --interactive --reuse "bash"
  logs: app logs -f
  dbc: app exec --interactive --reuse "bin/rails dbconsole"


# Use a persistent storage volume for sqlite database files and local Active Storage files.
# Recommended to change this to a mounted volume path that is backed up off server.
volumes:
  - "internalq_storage:/rails/storage"


# Bridge fingerprinted assets, like JS and CSS, between versions to avoid
# hitting 404 on in-flight requests. Combines all files from new and old
# version inside the asset_path.
asset_path: /rails/public/assets

# Configure the image builder.
builder:
  arch: amd64

  # # Build image via remote server (useful for faster amd64 builds on arm64 computers)
  # remote: ssh://docker@docker-builder-server
  #
  # # Pass arguments and secrets to the Docker build process
  # args:
  #   RUBY_VERSION: 3.3.0
  # secrets:
  #   - GITHUB_TOKEN
  #   - RAILS_MASTER_KEY

ssh:
  user: ec2-user
  keys:
    - ~/Downloads/internalq.pem


# Use accessory services (secrets come from .kamal/secrets).
accessories:
  redis:
    image: redis:7.0
    host: 13.206.218.103
    port: 6379
    directories:
      - data:/data